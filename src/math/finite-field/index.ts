/**
 * math/finite-field — the DIRECT F_p×F_p view: a curve over F_p drawn as its
 * literal integer solutions in the projective plane P²(F_p), embedded flat in
 * ℝ³. Distinct from math/arithmetic, which models E(F_{p^k}) abstractly as
 * ℂ/Λ for the Hopf construction. Pure, zero-dependency.
 */
export * from './field'
export * from './curve'
export * from './plane'
