/**
 * math/families — profile-curve families and the τ solver (DESIGN.md §5.4).
 * Realizable region {0 < Re ≤ ½} ∖ {|τ−½| < ½}; strata: latitude circles (closed form),
 * equatorial wavy (1-param), general wavy (2-param Newton). Returns Candidate[].
 */
export {}
